{
    daemon: tsc -w
    daemon: sass --watch scss:css
    daemon: parcel watch --no-hmr --out-dir docs        \
                                    index.html          \
                                    view/index.html     \
                                    edit/index.html
}

docs/** {
    daemon: devd -m ./docs
}